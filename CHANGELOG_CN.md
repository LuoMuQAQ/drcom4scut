# 更新日志 (CHANGELOG)

## [0.3.6] - 2026-09-25（HeroUI / UDP 修订）

### 界面与字体

- 主窗口、安装器、卸载器统一为 HeroUI v3 风格的 Rust / Win32 原生界面，保留原版 logo。
- 全局内嵌开源 Noto Sans SC Regular / Medium，放大说明文字，改善高 DPI 下的小字显示；无需另行安装字体。
- 仅账号、密码输入框显示蓝色焦点框；点击空白处取消焦点。其他控件保留键盘操作，不绘制蓝框。
- 连接按钮与输入框统一 12 DIP 圆角；修复局部重绘闪烁与圆角锯齿，支持深浅色及高 DPI。

### UDP 重连

- 修复旧 EAPtoUDP 接收线程可能抢走重建后 SUCCESS 的竞争：每代 UDP 使用独立消息队列，订阅与缓存重放同步处理。
- STOP/SLEEP/QUIT 使成功缓存失效，避免重复使用过期认证；登录前清理旧校验值，等待支持超时与取消，退出唤醒空闲线程。
- 补充 UDP 接收错误、等待 SUCCESS 超时的日志识别；保留无有效 UDP 心跳时的进程恢复保护。
- 保留此前报文边界检查、读超时分类、重发空转修复与工作线程恢复。

### 验证与限制

- 187 项 GUI/安装相关测试、13 项核心离线测试、3 项快照测试通过，覆盖深浅色与 100%～200% DPI。
- 真实校园网断网恢复、睡眠唤醒和长期运行仍待实机验证；Windows 10022 的底层原因尚未确定。
- EAP 网卡重建仍可能遗留 pnet channel 句柄，由进程级重启回收。
- 发行版本仍为 0.3.6；本次修订源码标签为 windows-v0.3.6-heroui，原 windows-v0.3.6 标签保留。更新前先退出旧托盘实例。

## [0.3.5] - 2026-09-23

### 🎉 重大改进

#### 睡眠唤醒自动重连 ⭐
**问题：** 电脑睡眠后唤醒，软件无法自动重连或重连时间过长（30-60秒）

**修复：**
- 优化网络状态检测，正确识别睡眠唤醒场景
- 改进设备重新获取策略，使用智能指数退避（1秒→2秒→4秒→8秒）
- 增加 Socket 超时保护，避免永久阻塞
- 优化路由表就绪检测，不再误判网络状态

**效果：** 睡眠唤醒后 **2-10秒内自动重连**（原 30-60秒）

### ✨ 稳定性提升

#### 网络错误处理
- 为所有 Socket 操作添加 30 秒超时，避免程序"卡死"
- 区分临时网络错误和永久错误，智能重试
- 增加 Socket 有效性主动检测，快速发现失效状态
- 优化设备通道发送逻辑，防止无限重试

#### 并发性能优化
- 改进多线程锁争用策略，使用超时机制替代无限等待
- 优化自旋等待算法，减少 CPU 空转
- 增强线程间通信的错误恢复能力

### 🚀 性能优化

#### 安装程序
- 优化界面刷新逻辑，只在状态变化时重绘
- CPU 使用率从 3-5% 降至 **< 1%**
- 安装过程更流畅，响应更快

#### 主程序
- 减少不必要的锁竞争
- 优化日志写入性能
- 降低空闲时 CPU 使用率

### 🔧 技术改进

#### 网络层
- Socket 读超时：30 秒
- Socket 写超时：5 秒
- 路由等待：最多 3 次（共 3 秒）
- 设备重获：指数退避，最快 1 秒

#### 并发控制
- RwLock 获取超时：100 毫秒
- Device 发送重试：100 次（共 10 毫秒）
- Socket 健康检查：5 次连续错误后触发

### 📊 性能对比

| 指标 | 0.3.4 | 0.3.5 | 改善 |
|------|-------|-------|------|
| 睡眠唤醒重连 | 30-60秒 | 2-10秒 | **83% ↓** |
| 首次设备重获 | 15秒 | 1秒 | **93% ↓** |
| 安装程序 CPU | 3-5% | <1% | **80% ↓** |
| Socket 阻塞风险 | 无限期 | 最多30秒 | **已消除** |

### 🐛 已知问题

1. **Npcap 驱动要求**
   - 安装时仍需手动确认 Npcap 许可协议（Windows 限制）
   - 建议在安装前检查驱动状态

2. **首次连接延迟**
   - 冷启动时首次连接可能需要 3-5 秒（正常现象）
   - DNS 解析和设备初始化需要时间

3. **日志文件大小**
   - 长期运行后日志文件可能较大
   - 建议定期清理旧日志（程序会自动轮转）

### 📝 升级说明

#### 从 0.3.4 升级
- **推荐：** 直接安装新版本，会自动覆盖旧版本
- **便携版：** 下载新版本，删除旧版本目录
- **配置保留：** 账号密码和设置自动迁移

#### 从 0.3.3 及更早版本升级
- **必须：** 完全卸载旧版本后再安装 0.3.5
- **配置迁移：** 需要重新填写账号密码

### ⚠️ 注意事项

1. **管理员权限**
   - 程序需要管理员权限访问网络适配器
   - 首次运行时请允许 UAC 提示

2. **防火墙设置**
   - 确保防火墙允许程序访问网络
   - Windows Defender 可能需要手动添加例外

3. **睡眠设置**
   - 建议在电源选项中允许网络适配器在睡眠时断电
   - 唤醒后网络恢复更快

### 🔍 问题排查

#### 睡眠后仍不重连？
1. 检查日志文件（位于 `%LOCALAPPDATA%\drcom4scutGUI\logs`）
2. 查找 "Successfully reacquired ethernet device" 字样
3. 确认防火墙没有阻止程序
4. 尝试以管理员身份运行

#### 重连速度慢？
1. 检查网络适配器驱动是否最新
2. 确认 DNS 设置正确
3. 临时禁用防火墙测试
4. 查看日志中的重试次数

#### CPU 使用率高？
1. 确保安装程序已关闭
2. 检查核心进程是否频繁重启
3. 查看日志是否有大量错误信息

### 🙏 反馈与支持

#### 报告问题
- **GitHub Issues：** https://github.com/LuoMuQAQ/drcom4scut/issues
- **提供信息：**
  - 操作系统版本
  - 网卡型号
  - 完整日志文件
  - 重现步骤

#### 功能建议
欢迎在 GitHub Discussions 提出功能建议和改进意见。

---

## [0.3.4] - 2026-09-23

### 新增
- 界面状态显示优化：EAP 认证成功后显示"已连接"
- 进程管理改进：GUI 关闭时核心发送 EAPOL-Logoff 下线

### 修复
- 修复停滞重启机制：180 秒无 UDP 心跳时重启核心

### 技术
- 新增 `supervisor.rs` 模块监控父进程
- 核心退出前发送 Logoff 包（最多等待 1 秒）

---

## [0.3.3] - 2026-09-XX

### 修复
- 睡眠恢复后的重连问题（部分修复）
- UDP 停滞检测机制

### 已知问题
- 睡眠恢复后可能出现 Windows 错误 10022
- UDP 心跳停滞后的恢复不够快

---

## [0.3.2] - 2026-XX-XX

### 初始版本
- 基础连接功能
- GUI 界面
- 自动重连

---

**最新版本：** 0.3.5  
**发布日期：** 2026-09-23  
**下载地址：** https://github.com/LuoMuQAQ/drcom4scut/releases
